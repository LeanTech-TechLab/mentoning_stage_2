import { TextBgModificatorDirective } from "./text-bg-modificator.directive";

describe("TextBgModificatorDirective", () => {
  it("should create an instance", () => {
    const directive = new TextBgModificatorDirective();
    expect(directive).toBeTruthy();
  });
});
