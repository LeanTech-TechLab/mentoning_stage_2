import { ModifyTextPipe } from "./modify-text.pipe";

describe("ModifyTextPipe", () => {
  it("create an instance", () => {
    const pipe = new ModifyTextPipe();
    expect(pipe).toBeTruthy();
  });
});
